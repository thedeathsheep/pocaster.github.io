name: Deprecated GitHub Pages workflow (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disable GitHub Pages publish
        run: echo "GitHub Pages auto-deploy is disabled. Use deploy-farawayfromicu.yml."
